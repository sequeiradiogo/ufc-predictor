name: Weekly Predictions

on:
  schedule:
    - cron: '0 12 * * 5'  # Every Friday at 12:00 UTC
  workflow_dispatch:

permissions:
  contents: write

jobs:
  predict:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          playwright install chromium --with-deps

      - name: Download DB artifacts
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release download data-artifacts-latest --dir db/ --pattern "*.db"

      - name: Download rankings
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release download data-artifacts-latest --dir raw_data/ --pattern "rankings_history.csv"

      - name: Refresh live rankings snapshot
        id: refresh_rankings
        continue-on-error: true
        run: python scrapers/ufc_rankings_web.py

      - name: Upload updated rankings to release
        if: steps.refresh_rankings.outcome == 'success'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload data-artifacts-latest raw_data/rankings_history.csv --clobber

      - name: Run event predictions
        run: python scripts/predict_event.py --model ensemble --skip-existing

      - name: Commit predictions
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add predictions/
          if git diff --cached --quiet; then
            echo "No new predictions to commit."
          else
            git commit -m "chore: add event predictions [skip ci]"
            git push
          fi
