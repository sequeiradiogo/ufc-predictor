# UFC Predictor — Python dependencies
# Install with: pip install -r requirements.txt

# PyTorch CPU-only wheel index (avoids the 2GB CUDA download in CI)
--extra-index-url https://download.pytorch.org/whl/cpu

# Core data
pandas==3.0.3
numpy==2.4.6

# Machine learning
# Pinned exactly (not >=) -- models_v1(_prod)/*.joblib were trained/pickled
# against these versions. An unpinned install drifts to whatever's newest at
# CI run time, which silently corrupts calibrated ensemble probabilities on
# load (confirmed: sklearn logged InconsistentVersionWarning on every
# calibrated component, and CI's lightgbm 4.7.0 vs the trained 4.6.0 produced
# saturated 98-100% confidences here where the pinned versions gave ~79%).
scikit-learn==1.8.0
xgboost==3.2.0
lightgbm==4.6.0
torch==2.12.0
joblib>=1.3        # model serialisation (ships with scikit-learn, listed explicitly)
optuna>=3.0        # hyperparameter tuning (python ML_models/XGBoost.py --tune)

# Visualisation
matplotlib>=3.8
seaborn>=0.13

# API
fastapi>=0.110
uvicorn>=0.29      # ASGI server: uvicorn api:app --reload
httpx>=0.27        # required by fastapi.testclient.TestClient (tests/test_api.py)

# Scraping (refresh_data.py --auto)
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0          # faster HTML parser for BeautifulSoup
playwright>=1.44   # headless Chromium for Cloudflare-protected sites (run: playwright install chromium)
kaggle>=1.6        # kaggle datasets download (optional -- rankings scraper)

# Testing
pytest>=8.0
